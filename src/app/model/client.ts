import { ContactDetails } from "./contact-details";
import { Identity } from "./identity";

export class Client {
  id: number=0;
  name: string='';
  prenom: string='';
  login: string='';
  motDePasse: string='';
}